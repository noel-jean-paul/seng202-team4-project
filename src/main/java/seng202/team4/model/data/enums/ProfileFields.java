package seng202.team4.model.data.enums;

/**
 * enum for the different field of a profile.
 */
public enum ProfileFields {
    /**Names of profile attributes in the database */
    firstName, lastName, dateOfBirth, height, weight, pictureURL
}
